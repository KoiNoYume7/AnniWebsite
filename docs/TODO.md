# AnniWebsite — TODO / Ideas

> ⚠️ **Note:** The organizer roadmap (Phases 0-5) that used to live here has been **archived**.
> The organizer was migrated to **ALOS** (`alos.yumehana.dev`). All future organizer development
> happens in the `AnniLifeOS` repo. See `AnniLifeOS/README.md` for the current roadmap.

---

## Active TODO (AnniWebsite only)

- [ ] Remove `#/organizer` route and the organizer module from the codebase (deprecated, redirects to ALOS)
- [ ] Remove `organizer.css` once the organizer module is gone
- [ ] Re-implement devlog preview section on the homepage — latest 2-3 cards with a "→ All posts" link, same pattern as featured projects
- [ ] Homepage refresh — make it more compelling for all audiences (friends, potential employers, strangers discovering it)
- [ ] Update SC Tools references from CZTimers → AnniSCTools throughout

## Ideas

- Devlog "seen / unseen" marks — subtle dot on unread cards, tracked per user in AnniCore DB (or localStorage for logged-out)
- GitHub contribution heatmap widget on home page
- Public roadmap page (`#/roadmap`) auto-rendered from this TODO
- Pi telemetry pulse on home — tiny CPU temp / uptime indicator, read-only, no auth

---

*Organizer phases 0-5 archived — see git history if needed.*
